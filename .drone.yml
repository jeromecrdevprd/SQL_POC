kind: pipeline
type: docker
name: default
   
steps:    
  - name: clean
    image: mcr.microsoft.com/dotnet/framework/sdk:latest
    commands:
      - msbuild Database.sln
      
  - name: build
    image: mcr.microsoft.com/dotnet/framework/sdk:latest
    commands:
      - msbuild Database.sln